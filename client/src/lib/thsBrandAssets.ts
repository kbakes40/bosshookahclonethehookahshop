/** Hookah silhouette mark (matches thehookahshop.com favicon / age-gate art). Imported so Vite emits a hashed URL and deploys cannot serve a stale `/brand-icon.png`. */
import thsLogoMarkUrl from "../assets/ths-logo-mark.png";

export const THS_LOGO_MARK_SRC = thsLogoMarkUrl;
