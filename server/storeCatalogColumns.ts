/**
 * Columns required for storefront grouping + Product cards (excludes heavy/unused fields).
 */
export const BH_PRODUCT_STOREFRONT_LIST_COLUMNS =
  "id,name,brand,category,price,sale_price,sku,badge,in_stock,featured,trending,image_url,description,weight_lb,created_at" as const;
